name: Test

on:

  workflow_dispatch:

  
  
jobs:
  test:
    runs-on: ubuntu-22.04

    steps:
    - name: Test2
      if: always()
      run: |
        dpkg -l chromium-browser
        dpkg -l python3
        
    - name: Test
      if: always()
      run: |
        apt list --all-versions python3 chrome
        apt list --all-versions chromium-browser
